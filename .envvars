# Global environment variable definitions
# This should be sourced by all bourne-compatible shells.

# Locale settings
if [ $(uname -s) != "Darwin" ]; then
    export LANG="en_US.UTF-8"
fi

# Default programs
export EDITOR=vim
export PAGER=less
